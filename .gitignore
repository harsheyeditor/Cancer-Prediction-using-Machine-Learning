# Virtual Environment
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Jupyter Notebook
.ipynb_checkpoints

# Environment variables
.env
.env.local

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo

# Project specific ignores
# Note: In a real project, we might ignore data/, but for a portfolio we might keep raw data if it's small/public.
# Let's ignore all generated data and models since we can reproduce them from sklearn.
data/raw/
data/processed/
models/
images/
